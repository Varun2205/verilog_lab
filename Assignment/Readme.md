PROBLEM STATEMENT:

1. Design a 4-bit adder with carry lookahead generator and write the Verilog code
for the same with gatelevel modeling.
2. Design a 4 bit magnitude comparator and write the Verilog code for the same
with dataflow modeling.
3. Design a decade counter and write its verilog code with behavioral modeling.
4. Implement the following function F(A,B,C,D) = (1,3,4,11,12, 13,14,15) using
multiplexer. Write the Verilog code for the same.
5. Draw the state diagram for detecting a non-overlapping sequence of 10110 with
mealy model and write the Verilog code for the same.
6. State difference between
a) Blocking and non -blocking statement
b) Task and functions
c) Always and initial statements
d) Continuous assignment delay, implicit assignment delay and net declaration
delay with examples